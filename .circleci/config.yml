version: 2.1
jobs:
  build:
    machine: true
    steps:
      - checkout
      - run: |
          echo "$DOCKER_PASSWORD" | docker login --username $DOCKER_LOGIN --password-stdin
      - run: docker build -t registry.heroku.com/$HEROKU_APP/web .
workflows:
  build-and-deploy:
    jobs:
      - build